{
    "name": "@fullevent/init-event",
    "version": "0.0.1",
    "description": "CLI tool to initialize FullEvent SDK",
    "main": "dist/index.js",
    "bin": {
        "init-event": "dist/index.js"
    },
    "scripts": {
        "build": "tsup src/index.ts --format esm,cjs --dts --clean",
        "dev": "tsup src/index.ts --format esm,cjs --watch --dts",
        "prepublishOnly": "npm run build"
    },
    "dependencies": {
        "prompts": "^2.4.2",
        "picocolors": "^1.0.0",
        "commander": "^11.1.0"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "@types/prompts": "^2.4.9",
        "tsup": "^8.0.1",
        "typescript": "^5.3.2"
    }
}