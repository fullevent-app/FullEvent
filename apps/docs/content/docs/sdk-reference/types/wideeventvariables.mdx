---
title: WideEventVariables
description: Type for Hono context variables.
---

# WideEventVariables

Type for Hono context variables.

Use this to type your Hono app for proper TypeScript support.

## Usage

```typescript
import { Hono } from 'hono';
import { wideLogger, WideEventVariables } from '@fullevent/node-sdk';

const app = new Hono<{ Variables: WideEventVariables }>();

app.use(wideLogger({ apiKey: '...', service: 'my-api' }));

app.get('/', (c) => {
  const event = c.get('wideEvent'); // Properly typed!
  event.user_id = 'usr_123';
  return c.text('Hello!');
});
```

## Properties

<APIReference>

<APIMethod name="WideEventVariables.wideEvent" category="WideEventVariables">
<APIMethodContent>
<APIMethodLeft>
<APIDescription>
The wide event for the current request

</APIDescription>
<APIParameters>
<APIParam name="wideEvent" type="WideEvent" required>
The wide event for the current request
</APIParam>
</APIParameters>
</APIMethodLeft>
<APIMethodRight>
<APISignature>{`wideEvent: WideEvent`}</APISignature>

</APIMethodRight>
</APIMethodContent>
</APIMethod>

</APIReference>
